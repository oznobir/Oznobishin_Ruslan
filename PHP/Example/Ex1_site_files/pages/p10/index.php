<?php
if (is_numeric($s) && is_numeric($d)) {
    $sk = $s * 2;
    $n = 0; // номер года, вначале 0
    while ($s < $sk) {
        $n++;
        $pn = $s * $d / 100; // начисленные проценты
        $s += $pn;
        echo 'за ' . $n . ' год % ' . $pn .
            ', общая сумма: ' . $s, "<br>";
    }
} else {
    echo "<p>Не числовые данные</p>";
}